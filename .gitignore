# ===== Next.js / node =====
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions
/coverage
/.next/
/out/
/build
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
.env*
# 예외: 값이 아니라 **키 이름 목록**이다. 환경변수 카탈로그라 추적해야 의미가 있다.
!.env.example
.vercel
*.tsbuildinfo
next-env.d.ts

# ===== 로컬 작업물 — 공개 사이트 repo에서 제외 (디스크에는 유지) =====
.DS_Store
.playwright-mcp/
_shots/
_bmad/
_bmad-output/
.claude/
docs/*
# 예외: 말투 규칙은 CI(post-drafts)가 읽어야 해서 추적한다.
# docs/ 를 통째로 제외하면 git이 디렉터리로 내려가지 않아 이 예외가 안 먹는다.
!docs/voice.md
# ADR 은 코드 독자를 위한 결정 기록 — 추적한다 (2026-09-18)
!docs/adr/
!docs/adr/**
# 기능을 켜는 설정 가이드 — 코드 독자가 읽어야 한다 (2026-09-21)
!docs/SETUP-features.md
# 실측·한계 기록 — cron 이 돌 때마다 한 줄씩 붙는다. voice.md·adr/ 과 같은 이유로 추적 (2026-09-21)
!docs/limits.md
# korean-humanizer 사본 — write_thread.humanize 가 러너에서 읽는다. 빠지면 윤문이 조용히 꺼진다(2026-09-24 #93)
!docs/humanizer/
!docs/humanizer/**
DESIGN-mintlify.md

# 별도 콘텐츠 작업물 (공개 repo 제외)
quitting-corporate-to-solopreneur/

# python
__pycache__/

# 영상 편집 산출물 (수백 MB)
out/
