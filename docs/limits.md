# 한계 분석 — 2026-09-10 구현 웨이브

네 도구를 전부 실데이터로 한 번씩 돌린 결과다. "다음에 할 것"으로 얼버무리지 않고,
항목마다 **시도한 접근 / 막힌 지점 / 우회 가능 여부 / 우회 비용**을 적는다.
증거는 실행 로그·산출 파일 경로다. 증거 없는 항목은 "미검증"으로 남긴다.

## 1. X 핵심 추출 (`scripts/x_core.py`)

| | 내용 |
|---|---|
| 시도 | syndication 무인증 경로(기존) + Bluesky 폴백. 링크 군집 → 점수 → LLM 한 줄 |
| 실측 | 2026-09-10 18:30 KST: 회전 대상 X 핸들 3개 **전부 429**. Bluesky 4계정 16건 수집 → `digest/x-2026-09-10.md` 4건 |
| 막힌 지점 | (a) X 무료 경로는 사실상 죽었다 — IP당 2~3회 후 429, 쿨다운 13분에도 안 풀림. (b) 상한 없이는 한 계정(404media)이 7/7 독식 → 저자당 2건 상한 추가 |
| 우회 | **X API v2 종량제** (`X_BEARER_TOKEN`). 코드 경로는 있으나 **토큰이 없어 미검증**. 하루 200건 읽기 ≈ 월 $1 미만 (https://docs.x.com/x-api/getting-started/pricing) |
| 우회 비용 | X 개발자 계정 + 크레딧 선구매. 약 30분 + 월 $1~5. 쿠키 스크래핑은 약관 위반이라 **공개 repo 에 넣지 않는다** |
| 남는 한계 | "핵심"은 점수(좋아요·저자 수)로 고른다. 좋아요가 적은 초기 신호는 놓친다 — 이건 설계상 감수 |

## 2. 영상 편집 자동화 (`scripts/edit_video.py`)

| | 내용 |
|---|---|
| 시도 | ffmpeg + mlx_whisper(large-v3-turbo) + LLM 블록 선택 + select/aselect 1패스 컷 |
| 실측 | `Seungbin_Min_HKU.mp4` 407s → 전사 136문장 23s → 19블록 중 7 유지(212.8s, 52%) → `out/hku_auto/{cut.mp4, cut_sub.mp4, cut.srt, thumb.jpg, short_9x16.mp4}` 총 98s. claude 한도 중이라 **ollama qwen2.5:7b 폴백이 블록을 골랐다** — 폴백 경로가 실제로 작동함을 확인 |
| 막힌 지점 1 | Homebrew `ffmpeg` 8.1.1 은 libass·freetype 없음 → 자막 번인·썸네일 텍스트 불가. `ffmpeg-full` 9.0.1 설치로 해결했으나 **x265 가 4.3 으로 올라가며 기존 ffmpeg/ffprobe 링크가 깨짐** (`libx265.216.dylib` 없음). `brew reinstall ffmpeg` 로 복구. 스크립트는 이후 ffprobe 도 같은 빌드 경로를 쓴다 |
| 막힌 지점 2 | 썸네일 프레임이 **눈 감은 순간**에 걸렸다(`thumb.jpg`). 블록 중앙 시각을 기계적으로 뽑기 때문. 얼굴·눈 상태는 ffmpeg 단독으로 못 본다 |
| 막힌 지점 3 | 숏폼 9:16 은 정적 중앙 크롭 + 블러 배경. 발화자가 화면 가장자리로 움직이면 잘린다 |
| 우회 | 2·3 모두 mediapipe/YOLO 얼굴 검출 필요 (pip 1개 + 모델). 약 반나절. `# ponytail` 주석으로 승급 조건 표시함 |
| 미검증 | 한/영 혼용 화면녹화 전사 품질(테스트 영상은 발표 영상 1개). 컷 경계 오디오 클릭음 여부(귀로 안 들음) |
| 플랫폼 | mlx_whisper 는 Apple Silicon 전용. **GitHub Actions 에서는 못 돈다** — 이 도구만 로컬 실행 |

## 3. S급 블로그 자동 발행 (`scripts/write_post.py` + `auto-post.yml`)

| | 내용 |
|---|---|
| 시도 | 주제+출처 → sonnet 생성 → `grade.py --source`(수치 대조) → 재시도 1회 → PR → 머지 |
| 실측 | (아래 §실행 기록 참조) |
| "S급"의 정의 | 측정 가능한 것만: 출처에 없는 수치 0개·직접 인용 1개 이상·voice.md 7금지 0건·LLM 판정 PASS. 근거는 Princeton GEO(인용·통계 +30~40%)와 Google 가이드 — 그러나 **이게 실제 조회·구독으로 이어지는지는 발행 후 7일 지표로만 알 수 있다**. 오늘은 그 지표가 없다 |
| 막힌 지점 | GITHUB_TOKEN 이 만든 PR 은 `pull_request` 워크플로를 안 깨운다 → 채점·머지를 같은 job 에서 끝내는 구조로 우회 (auto-merge 설정 불필요) |
| 우회 비용 | 없음. 교차 발행(dev.to·Hashnode)은 범위 밖 — 티스토리·Medium·velog·브런치는 API 가 죽었거나 없다 |
| 남는 한계 | 수치 대조는 "문자열이 출처에 있는가"만 본다. 있는 숫자를 엉뚱한 맥락에 붙이는 오류는 LLM 판정에 의존 |

## 4. S급 스레드 자동 발행 (`scripts/write_thread.py`)

| | 내용 |
|---|---|
| 시도 | 글/다이제스트 → sonnet 초안(500자, 넘치면 체인 3개) → 각 글 채점 → `post_text(reply_to_id)` 체인 발행 |
| 실측 | (아래 §실행 기록 참조) |
| 막힌 지점 | **THREADS_TOKEN 이 없다** → 발행은 DRY_RUN 까지만 검증. Meta 앱 생성(dev mode, 심사 불필요) + 장기 토큰 발급이 선행 |
| 우회 비용 | developers.facebook.com 에서 약 20분. 토큰 60일 만료는 `refresh-threads-token.yml` 이 갱신하되 **GH_PAT Secret 이 추가로 필요** |
| 남는 한계 | 한국 개발자 니치에서 어떤 형식이 반응이 좋은지 **근거 있는 자료가 없다**. 채점기는 "내 목소리인가"만 보고 "반응이 올까"는 못 본다 |

### 4-1. 실발행 기록 (2026-09-14, THREADS_TOKEN 등록 후)

| 시도 | 결과 | 원인·조치 |
|---|---|---|
| 1 | 채점 2회 탈락, 발행 안 감 | 판정기가 리듬·대조 취향으로 탈락시킴 → 훅·종결·금지어만 FAIL 사유로 (#22) |
| 2 | 채점 PASS → 컨테이너 400 | 본문 미출력 → 에러 본문 노출 (#23) |
| 3 | 400 `Object with ID does not exist` (code 100/33) | 손으로 넣은 THREADS_USER_ID ≠ 토큰 주인 → 경로 `me`, Secret 제거 (#24) |
| 4 | **1번 글 발행됨** `18087042818682757` → 답글 컨테이너 500 (본문 없음) | 발행 직후 타이밍 가설 → 15초 대기 + 500 재시도 3회 (#25) |
| 5 | 3분 뒤 재시도도 500 ×3 | 타이밍 아님. 원인: 토큰에 `threads_manage_replies` 없음 — 공식 문서 "답글 엔드포인트 POST 에 필요" |
| 6 | **권한 추가 + 토큰 재발급 후 2·3번 답글 발행 성공** | `18129159433751645`, `17953051092250970` → https://www.threads.com/@inpilot.dev/post/DdRJrGYm9vX |

확정된 것: 단일 글·체인 모두 된다. 토큰에 필요한 권한은 `threads_basic` + `threads_content_publish` + `threads_manage_replies` 3개. 체인이 끊기면 `--reply-to <ID> --start N` 으로 잇는다.

### 4-2. 첫 cron(9/16) 전에 찾아 고친 것 (2026-09-15)

| 결함 | 원인 | 조치 |
|---|---|---|
| 다이제스트에서 **이미 글로 쓴 출처를 다시 고를 수 있었다** → 같은 slug 덮어쓰기 + 스레드 재발행 | 브랜치를 매일 main 에서 새로 따서 `.seen.json` 이 이월되지 않는다. 실측: 9/14치 36건 중 13건이 9/13과 같은 URL, 그중 astra 출처는 9/14 #19 로 이미 게시됨. `pick_from_digest` 는 쓴 글을 모르고, `write_post` 는 slug 가 같으면 덮어쓴다 | PR #28: `content/*.mdx` 에 인용된 URL 은 주제 판정 전에 제외 + 쓴 출처를 frontmatter `sources:` 에 기록(LLM 이 본문 URL 을 빠뜨려도 잡힘). 실측: 9/14 다이제스트 36건 중 astra 1건 제외. `.seen.json` 이월은 안 고쳤다 — deferred-work 의 "안 올린 건 다시 보인다"는 의도된 동작 |

## 5. 부수 트랙 — 수익화·결제 (조사만)

구현하지 않았다(요청 범위 밖). 조사 결과는 `docs/research-2026-09-10.md` §5 에 있다 — 그 파일은
`docs/*` 로 제외돼 **이 공개 저장소에 없다**. 이 문서는 추적되므로 사업 쪽 세부는 여기 적지 않는다.

## 6. 이번 웨이브에서 바뀐 시스템 상태 (되돌릴 수 있게)

| 변경 | 되돌리기 |
|---|---|
| `brew install ffmpeg-full` (keg-only, `/opt/homebrew/opt/ffmpeg-full`) | `brew uninstall ffmpeg-full` |
| `brew reinstall ffmpeg` (x265 링크 복구) | 불필요 |
| `out/hku_auto/` 산출물 330MB | `rm -rf out/` (gitignore 대상) |

## 실행 기록 (2026-09-10 18:30 ~ 09-11 13:45 KST)

| 도구 | 입력(실데이터) | 결과 | 증거 |
|---|---|---|---|
| 채점기 | selftest | ok | `python3 scripts/grade.py --selftest` |
| X 핵심 | X 핸들 3 + Bluesky 4 | X 전부 429 · Bluesky 16건 → 4건 | `digest/x-2026-09-10.md` |
| 영상 | `Seungbin_Min_HKU.mp4` 407s | 19블록→7 유지, 5개 산출, 98s | `out/hku_auto/report.json` |
| 블로그 | 주제 "X 크롤링은 되는가" + 출처 2파일 | 1차 FAIL(대조 약함) → 2차 PASS → PR #11 squash 머지 | `content/x-crawling-429-pay-per-use.mdx` · https://github.com/inpilot-dev/seungbin-dev/pull/11 · 커밋 `a29a55c` |
| 스레드 | `content/ponytail-lazy-senior-dev.mdx` | 1차: 판정기가 블로그 규칙(`> 한 줄 요약`)을 스레드에 적용해 FAIL → 기준 분리 후 PASS, 2개 체인 DRY_RUN | `drafts/threads/ponytail-lazy-senior-dev.md` |

채점기 자체의 결함 1건이 실행 중 드러났다(스레드에 긴 글 규칙 적용). 판정기도 검수 대상이라는 원칙 그대로 — 발견 즉시 종류별 기준으로 고쳤다.
블로그 글은 `docs/crawling-plan.md`·`docs/research-2026-09-10.md` 의 실측 수치만 썼고, 채점기가 그 수치 전부를 출처 문자열로 대조해 통과시켰다.

- **2026-09-16**: daily-digest 의 PR 생성 중단(브랜치 커밋 + 14일 지난 digest 브랜치 자동 삭제만 남김), `publish-threads.yml` 삭제. "체크 + 논평" 수동 발행 경로는 누적 0회 — 실발행은 전부 `grade.py` 경로였다. `scripts/publish_threads.py` 는 `write_thread.py:29` 가 `post_text` 를 import 해서 남긴다. Secret `THREADS_USER_ID` 삭제(PR #24 이후 미사용).
- **2026-09-16 auto-post 첫 cron (run 35047554531, 11:19 KST 실행)** — 사람이 막았어야 했나: **아니오. 채점기가 막았다.**
  - PR #28 반영 확인: `2026-09-16.md: 36건 중 이미 쓴 출처 0건 제외, 주제 적합 9건`(당일 첫 수집이라 제외 0이 정상). 로컬 `x.md` 셀프테스트에서는 제외 1건이 찍혀 필터 자체는 동작.
  - 주제: Astra 원샷 픽셀아트 → 시도 1 FAIL(원문에 없는 수치 `63` — 지어낸 숫자), 시도 2 FAIL(규칙 1 결론 지연 · 규칙 5 문장 전체 볼드 · 규칙 18 1인칭 약함). `drafts/astra-one-shot-pixel-art-knights.mdx` 에만 남고 발행 0건.
  - 워크플로는 exit 1(빨간불)로 끝난다. 글이 안 나간 경우라 빨간불이 맞다 — 결함3(글이 나간 뒤 스레드만 떨어져도 빨간불)과는 다른 상황이다.
- **2026-09-16 저녁 — 9/22 예정이던 결함 4개를 앞당겨 닫음 (PR #31, `c5ff952`)**
  - 결함1: `grade.py` 가 마지막 `VERDICT` 줄만 봤다. 비평 본문에 규칙 위반이 적혀도 `PASS` 면 통과. 9/16 cron 은 우연히 마지막 줄이 FAIL 이라 막힌 것이지 이 구멍이 없어서가 아니다. `critique_violations()` 로 비평 본문을 훑어 FAIL 시킨다.
  - 결함2: 블로그 판정 기준에 규칙 18(1인칭 선언) 추가. 같은 원문의 스레드는 이 기준으로 FAIL 이 났는데 블로그엔 기준이 없었다.
  - 결함3: `auto-post.yml` 에서 "글은 나갔고 스레드만 채점 탈락"(경고 + 초안 아티팩트)과 "발행 체인 중단"(빨간불)을 갈랐다.
  - 결함4: `refresh-threads-token.yml` 주석 요일 정정(월 19:00 UTC = 화 04:00 KST).
  - **알려진 한계**: `critique_violations` 는 정규식이라 LLM 이 새 어휘를 쓰면 놓친다. 9/16 원문 기준으로 `어긋|부합하지 않|놓쳤|미흡|무시하` 등을 채웠고, 로그에 새 표현이 보이면 한 단어씩 늘린다. 셀프테스트에 9/16 비평 원문이 회귀 케이스로 들어가 있다.
- **2026-09-16 밤 — 서브에이전트 감사로 새 결함 6개 발견, 확인된 것 고침 (PR #32)**
  - **[가장 무거움] cron 에서 `MERGE` 가 false 였다.** `inputs` 는 workflow_dispatch 에서만 채워지고 GitHub 표현식이 타입 불일치를 숫자로 캐스팅(null→0, false→0)해 `inputs.merge != false` 가 false 가 된다. 9/16 cron 로그에 `MERGE: false` 실측. 무인 경로에서만 머지가 꺼져 있었고, 스레드 발행은 머지 여부와 연결이 없어 **404 링크를 단 스레드가 나갈 수 있었다.** 오늘은 채점 FAIL 로 거기까지 못 가서 안 터졌다. → `github.event_name` 으로 판정 + 스레드를 실제 머지에만 건다.
  - digest 브랜치 미발견 시 조용히 진행 → main 에 추적된 `digest/x-2026-09-10.md`(6일 묵음)로 글을 썼다. 이제 exit 1.
  - prune 하한 없음 → 브랜치를 안 만든 날에도 삭제가 돌았다. `steps.push.outputs.pushed` 로 건다.
  - `.FAIL` 초안이 `--draft` 로 발행 가능했다(결정론 검사만 통과하면 나감). 이름으로 거부. **게이트가 파일 이름뿐이라 rename 하면 여전히 뚫린다** — dispatch 권한자는 이미 repo 쓰기 권한자라 매니페스트는 안 만들었다.
  - `--start 0` → `parts[-1:]` 로 마지막 한 글만 조용히 발행. 범위 검사 추가.
  - 다이제스트 신선도 3일 추가(파일명 날짜 기준, mtime 은 git 이 보존 안 해 CI 에서 무의미).
  - 수치 대조 3중 누수 수정: `20xx` 전면 면제 → 실제 연도 범위, 원문에도 `strip_code`, 단위·소수점 보존. **남는 구멍: 원문 평문에 맨숫자가 있으면 초안이 붙인 단위는 안 따진다.** 단위까지 강제했더니 astra 글이 떨어졌다(원문이 영문 `took 27 minutes`). 한 자리 수치는 여전히 무검사.
  - 회귀: 발행된 글 42편 자기 원문 채점 시 수치 오탐 0. 자동 생성 글 2편 실제 원문 재채점 통과.
- **2026-09-16 밤 2차 — 적대적 검토가 내가 같은 날 머지한 것에서 과차단 5건을 찾았다 (PR #34)**
  - **교훈이 먼저**: `--selftest` 통과와 "파이프라인이 안 막힌다"는 다른 문제다. 셀프테스트는 내가 고른 케이스만 본다. 게이트를 건드릴 때는 **정상 입력이 계속 통과하는지**를 따로 봐야 한다.
  - **검증이 항등식이었다.** PR #32 의 "42편 자기 자신을 원문으로 채점해 오탐 0"은 초안 수치가 원문 수치의 부분집합이라 구조적으로 항상 0이다. 그래서 과차단이 안 잡혔다. 올바른 방법은 **초안만 변형**(단위 생략·표·꼬리 바꾸기)해 구버전과 대조하는 것. 결과: 구버전 126개 중 66 과차단 → 신버전 2(그 2건은 변형이 `100만 원`을 `100원`으로 바꿔 크기를 바꾼 정상 탐지).
  - R1: `NO_VIOLATION_RE` 가 한국어 부정 `않` 을 몰라 "어떤 규칙도 위반하지 않았다"를 위반으로 셌다. 정상 비평 14개 중 5개 오탐.
  - R2: 블로그에 권장 규칙 예외가 없어 판정 기준에 없는 규칙(5·16·20) 지적만으로 FAIL — "비평이 전부 칭찬이어야 통과"였다.
  - **해법: 산문 정규식을 버리고 판정 모델에게 `VIOLATIONS: <규칙 번호>` 한 줄을 더 받는다.** 한국어 부정을 정규식으로 맞히려 들지 말 것. 그 줄이 없으면 VERDICT 만 믿는다(형식 미준수로 전부 죽는 걸 막는다).
  - R4: 단위를 일치 조건에서 **충돌 신호**로 내렸다. 한쪽이라도 단위가 없으면 맨숫자 비교, 양쪽 다 있는데 다를 때만 FAIL.
  - R5: 원문 전처리를 `strip_code` → URL 제거만으로. `--post` 는 .mdx 전문을 원문으로 넘기는데 명령 출력·설정값이 코드블록에 산다.
- **2026-09-16 밤 3차 — "조용한 성공" 패턴을 저장소 전체에서 훑음 (PR #35)**
  - **상시 발생 중이던 것**: `.seen.json`(다이제스트 중복 제거 원장)이 `digest/<날짜>` 브랜치에만 커밋되는데 그 브랜치는 #30 이후 머지되지 않는다. CI 는 매번 새 체크아웃이라 원장이 늘 비었고 전건이 "신규"였다. 그래서 매일 전날 항목이 다시 실렸다(9/14치 36건 중 13건이 9/13과 동일 — 이미 실측돼 있던 수치). → daily-digest 가 최신 브랜치에서 원장을 가져온다. **가져온 원장이 커밋 판정을 오염시키지 않게 판정을 `.md` 기준으로 좁혔다** — 안 그러면 새 항목 없는 날에도 원장뿐인 브랜치가 생기고 prune 이 돈다.
  - `x_core.py`: 토큰이 있으면 폴백을 아예 안 탔다. 토큰이 죽으면 `x-*.md` 가 영영 안 생기는데 워크플로는 `continue-on-error` 라 초록불. → `or collect_fallback(...)`.
  - `publish_threads.py`: `except Exception → set()` 이 JSON 깨짐까지 덮어, 원장이 한 번 깨지면 **이미 나간 항목이 전부 재발행**된다. → `FileNotFoundError` 로 좁힘.
  - `collect_digest.py`: 전 소스 실패와 신규 0건이 같은 exit 0. → `collected` 가 비면 1.
  - `write_post.py`: 못 읽은 출처를 버리고 진행하면서 frontmatter `sources:` 엔 인용한 것처럼 남았다. → 하나라도 못 읽으면 2로 죽는다. cron 경로는 출처가 1개라 기존 `return 2` 가 먼저 걸려 영향 없음.
  - **안 고침**: `edit_video.py` 가 ffmpeg 종료 코드를 안 보고 실패를 "ffmpeg-full 없음"으로 단정한다. 발행 경로 밖 로컬 전용 도구.
- **2026-09-16 밤 4차 — 판정 기준 규칙 집합이 프롬프트와 어긋나 있었다 (PR #36). `claude -p` 실호출로 확인.**
  - **블로그에 규칙 13 이 빠져 있었다.** 프롬프트가 PASS 기준으로 선언한 "끝이 `> 한 줄 요약`" 이 voice.md 13번인데 `JUDGED_RULES["blog"]` 에 없었다. 모델이 실제로 `VIOLATIONS: 13` 을 쓰는데 무시됐다 — PR #31 이 막으려던 구멍이 규칙 13 에 대해 그대로 열려 있었다.
  - **스레드는 반대로 너무 넓었다.** 프롬프트 기준은 훅(1)·평서 종결(2) 둘뿐인데 8개가 더 들어 있었다. 5(볼드)는 Threads 가 평문이라 적용 불가, 18(1인칭)은 체인 파트별 채점이라 중간 파트가 구조적으로 못 지킨다. 실측 4/4는 `none` 이라 미발현이었지만 판정 모델이 바뀌면 지뢰. → `{1, 2}`.
  - **voice.md 는 `## 1~20` 규칙과 '이 목소리가 아닌 것' 7개가 같은 숫자 공간을 쓴다.** 모델이 금지 위반에 `VIOLATIONS: 1,2,4,5,7`(2인칭·느낌표·이모지·과장·합니다체)을 썼다. 규칙 번호와 구분 불가였다. → 프롬프트가 `R번호`·`F번호` 접두사를 쓰게 함.
  - 부정문 오탐이 VIOLATIONS 줄 안에 남아 있었다(`없음 (규칙 1~20 모두 준수)`). → 줄 안 "없음" 계열 + 접두사 없음이면 통과.
  - 정규식이 `**VIOLATIONS**:`·소문자·콜론 앞 공백을 놓쳐 안전망이 조용히 꺼졌다. → 대소문자 무시 + `**` 허용.
  - **교훈**: 프롬프트가 선언한 PASS 기준과 코드의 규칙 집합은 손으로 맞춘 두 목록이라 어긋난다. 한쪽을 고치면 다른 쪽을 반드시 대조할 것. `JUDGED_RULES` 주석에 1:1 대응을 적어 뒀다.
- **2026-09-16 밤 5차 — 실제로 한 번 돌려봤다. 셀프테스트로는 안 보이던 게 셋 나왔다 (PR #37)**
  - **발행 경로 실측 성공**: `DRY_RUN=1 write_thread.py --post content/x-crawling-429-pay-per-use.mdx --publish` 를 로컬 `claude` 로 돌렸다. 시도 1은 판정 모델이 `VIOLATIONS: R1` 을 내 FAIL(훅 약함), 되먹임 후 시도 2 PASS → 3파트 체인 생성 → DRY 발행. **PR #36 의 `R번호` 접두사 형식을 판정 모델이 첫 실전에서 그대로 따랐다.**
  - **`grade.py --selftest` 가 PR #36 에서 깨진 채 머지됐다.** 로그 문구를 "판정 기준 규칙 위반" → "판정 기준 위반" 으로 다듬으면서 assert 한 줄을 같이 안 고쳤다. 머지 전에 `| tail -3` 출력만 눈으로 보고 **종료 코드를 안 봤다.** 같은 날 메모리에 "셀프테스트 통과를 확인하라"고 적어 놓고 그 확인을 stdout 으로 했다. → 앞으로 `--selftest` 는 반드시 `; echo $?` 로 본다.
  - **`--source` 가 조용히 덮어썼다.** `nargs="*"` 라 `--source a --source b` 를 주면 `b` 만 남는다. 원문 절반으로 채점하고도 아무 경고가 없다. 실제로 당했다 — 원문에 있는 수치 6개(`101개·13분·286일·287일`…)가 "지어낸 숫자"로 찍혀 과차단인 줄 알았다. → `action="extend"`.
  - **안 고친 것 — `coverCmd` 본문 대조가 42편 중 39편에서 걸린다.** 사람이 쓴 글은 본문에 일반형(`.../screen-name/<핸들>`), 커버에 구체형(`.../karpathy`)을 쓴다. 검사(PR #17)는 리터럴 부분문자열이라 전부 실패한다. **자동 생성 글을 실제로 막은 적은 없다** — astra(#19)는 coverCmd 를 아예 안 썼고, x-crawling(#11)은 이 검사보다 먼저 발행됐다. 다만 생성 프롬프트가 기존 42편을 참고로 주므로 같은 패턴을 흉내 내면 걸릴 수 있다. **오늘은 안 고친다** — 채점기를 조일 때마다 그 수정이 또 문제를 만든 게 오늘만 다섯 번이다. 근거(39/42)를 남기고 판단은 미룬다.
- **2026-09-16 밤 6차 — 내일 03:00 daily-digest 를 임시 클론에서 리허설했다 (PR #38)**
  - **깨지는 곳 없음.** 오늘 넣은 세 변경(원장 가져오기 · `.md` 커밋 판정 · prune 하한)이 전부 의도대로 돈다. 단계별 실측: `LATEST=origin/digest/2026-09-16`, 원장 36건, prune CUTOFF `digest/2026-09-03` → **`digest/2026-09-02` 하나만 삭제**, 나머지 7개 + 내일치 유지. auto-post 가 읽을 최신 브랜치가 지워지는 경우 없음.
  - **원장 가져오기의 효과가 실측됐다**: 원장 36건 적용 시 신규 227건, 어제와 겹치는 것 **0건**. 원장 없이 돌리면 신규 248건, **어제 실린 것 9건이 재탕**. 개선이 실제로 문다.
  - 원격 8개 digest 브랜치 전부에 `.seen.json` 이 있다(`gh api contents` 확인). 스텝이 헛돌지 않는다.
  - `git reset -q digest/` 는 mixed 리셋이라 워킹트리의 `x-2026-09-10.md` 를 안 건드린다(md5 동일 확인). 판정 오염 없음.
  - **잠복 지뢰 하나 — 고쳤다.** `git status --porcelain` 은 디렉터리가 **통째로 untracked** 면 파일이 아니라 `?? digest/` 한 줄만 낸다. 그러면 `.md` 매치가 없어 다이제스트를 만들고도 "신규 항목 없음" 으로 초록불이 된다. 지금은 main 에 `digest/x-2026-09-10.md` 가 추적돼 있어 무사하지만 그 파일이 지워지는 날 이 게이트는 **영구히 침묵**한다. → `-uall` 한 플래그. 로컬 git 샌드박스로 양쪽 재현 후 적용.
  - **안 고친 것**: ① 같은 날 두 번 돌리면 `digest/<오늘>` 이 원격과 갈라져 push 가 non-fast-forward 로 거부된다 → job 빨간불. 시끄럽게 실패하므로 둔다. cron 1회/일이면 안 걸린다. ② collect 가 신규 0건인데 `x_core` 만 성공하면 날짜 다이제스트 없는 브랜치가 생긴다. `pick_from_digest` 가 `x-` 파일도 후보로 보고 신선도 가드가 있어 죽지 않는다.
- **2026-09-16 밤 7차 — 블로그 경로 앞단을 실측했다 (PR #39)**
  - 신선도 가드 확인: 워킹트리에 `x-2026-09-10.md`(7일 묵음)만 있을 때 `pick_from_digest` 가 `SystemExit: 가장 최근 다이제스트 … 7일 묵었다 (> 3일) — 글을 만들지 않는다` 로 죽는다. 의도대로 문다.
  - **뜻밖의 발견 — 같은 날짜면 5건짜리 X 다이제스트가 36건짜리 본 다이제스트를 항상 이겼다.** `pick_from_digest` 가 파일명 날짜로 정렬하는데 날짜가 같으면 파일명 역순이라 `x-2026-09-16.md` > `2026-09-16.md` 가 된다. 아무도 고른 적 없는 동작이고 정렬에서 떨어진 것뿐이다.
    - 실측(임시 클론 + 실제 `digest/2026-09-16` 브랜치 + 로컬 `claude`): 고치기 전 → `x-2026-09-16.md` 5건 중 적합 2건에서 선택, 고른 주제가 "AI의 안전장치가 제거되고…" 라는 404media **뉴스 헤드라인**이었다. 고친 뒤 → `2026-09-16.md` 36건 중 **적합 17건**에서 선택, 주제가 기술 내용으로 바뀌었다.
    - `x-` 는 소셜 게시물이라 뉴스·논평이 많다. PR #17 이 막으려던 "미시간대 데이터센터 타운홀" 사고와 같은 계열이다. → 같은 날짜면 본 다이제스트가 먼저, `x-` 는 본 다이제스트가 비었을 때 후보.
- **2026-09-16 밤 8차 — `coverCmd` 본문 대조를 뺐다 (PR #40). 유지/완화/삭제를 데이터로 판정했다.**
  - **정밀도 ~3%.** coverCmd 가 있는 41편 중 39편이 걸리는데, 유형을 분류해 보니 실제로 지어낸 명령은 **최대 1편**(`bmad sprint-status`). 나머지는 공백·따옴표 차이(1) · 축약기호 `…`/플레이스홀더(2) · 본문 명령 축약·재배열(4) · 본문에 소재는 있고 실행 래퍼를 씌움(22) · 본문에 문자열 근거 없음이지만 실재하는 명령(10, `next build`·`gh repo view`·사용자 스킬 등)이다. 이 검사가 재는 건 "사실인가"가 아니라 "리터럴로 복사했나"다.
  - **완화는 더 나쁜 거래였다.** 완화 규칙 6종을 42편 + 가짜 7건에 실제로 돌린 결과: 형식만 봐주는 규칙으로도 41편 중 **36편이 여전히 FAIL**. 코퍼스를 통과시킬 만큼 풀면(4자 이상 토큰 1개 매치) 가짜 7건 중 2건이 샌다. **통과율과 검출력이 같은 손잡이에 달려 있다.**
  - **생성 프롬프트가 자기 규칙을 어긴 예시를 준다.** `REFS = ["ponytail-lazy-senior-dev", "order-state-machine"]` 두 편 다 coverCmd 가 본문에 없다(후자는 등호 양옆 공백 한 끗). 모델은 규칙과 위반 예시를 같은 프롬프트에서 본다.
  - **관측된 효과가 의도와 달랐다.** 도입(#17) 이후 자동 생성 글을 막은 적 없고, 실제 효과는 "자동 글은 coverCmd 를 아예 빼서 터미널 커버를 못 갖는다"였다(#19가 그랬다).
  - **나중에 고칠 자리는 여기가 아니라 `coverOut`.** 검증 가능한 주장은 명령 문자열이 아니라 그 출력의 숫자인데, coverOut 41편 중 본문에 있는 건 1편이고 수치 대조는 frontmatter 를 아예 안 본다. `prose` 에 `meta.get("coverOut","")` 을 붙이면 기존 대조 기계가 공짜로 잡는다. **그것도 새 과차단 표면이라 실제 사건이 터진 뒤에 넣을 것.**
- **2026-09-16 밤 9차 — 블로그 경로를 처음으로 끝까지 실측했다 (임시 클론, 발행 없음)**
  - **오늘 추가한 규칙 13·18 기준이 생성을 막지 않는다.** 이게 오늘 내가 만든 가장 큰 미확인 위험이었다. `MAX_TRIES=2` 안에 통과한다.
    - 시도 1 FAIL: "규칙 18이 없다 — 1인칭 선언이 본문에 없고 Mollick 의 시도를 객관 서술만 한다". 판정 모델과 `VIOLATIONS` 이 일치.
    - 시도 2 PASS: `VIOLATIONS: none`. 첫 문장 결론 · `> 한 줄 요약` 마무리(규칙 13) · "나는 여기서 … 갔다"(규칙 18) 전부 충족. → `content/astra-one-shot-pixel-art-sprites.mdx`
  - PR #39(다이제스트 순서)도 실전 확인: `2026-09-16.md` 36건 중 적합 **15건**에서 골랐다. 고치기 전이었다면 `x-` 5건에서 골랐을 것이다.
  - 되먹임 재시도가 실제로 일한다 — 1차 비평의 지적을 2차가 그대로 고쳤다.
  - 임시 클론에서 돌려 실제 저장소는 손대지 않았다(글 42편 유지).
- **2026-09-18**: 3개월 자동화 계획 확정(원장 `#design`, decision-log 2026-09-18). 검수 표면 = GitHub PR(ADR 0001), 결정 없으면 보류. 9/23 cron 부터 머지 대신 PR(증분 1). n8n 10/01 판정 폐기 · PR #20 닫음. daily-digest 9/17 실행 정상 — 36건 + X 6건, PR 없음, `digest/2026-09-02` 만 삭제(리허설 예측 일치).
- **2026-09-18 15:15 증분 1 브랜치 dispatch(run 35318580668) 실패 — 코드가 아니라 사용량 한도.** `claude -p` 가 exit 1 로 즉시 죽었고 stderr 가 비어 있었다(한도 문구는 stdout). `on_topic` 이 None 을 전부 False 로 돌려줘 36건 전부 부적합 → "주제에 맞는 다이제스트 항목이 없다" 로 **오진**. 무인 cron 이 같은 날 같은 오진을 하면 아무도 모른다. → `c0d565d`: llm.ask 가 stdout 도 로그에 남기고 한도 문구면 5분 간격 2회 재시도(`LLM_LIMIT_RETRIES`), on_topic 은 무응답을 "판정 불가" 로 명시적으로 죽음, auto-post 실패 시 Telegram 한 줄. 16:00 리셋 뒤 재실행 예정.
- **2026-09-18 17:05 증분 1 머지(#41 → `e80fbc1`).** 재dispatch run 35322414732 는 한도가 아니라 **1번 후보 출처 HTTP 403(reddit)** 로 "출처가 비었다" 종료 → `34ded74` 기계 선택만 다음 후보로(`first_readable`). run 35322651602 ✅ 주제 적합 13건 → 2번으로 원고, 2회 FAIL → #42(`원고:blog`+`탈락`). 머지 후 #42 에 OWNER `/주제 3` → review.yml 35323455831 ✅ 시도 2 PASS → 같은 PR 브랜치 갱신(옛 drafts 삭제, `탈락` 라벨 제거, 결과 댓글). hold-timer dispatch 35323915740 ✅(대상 없음). #42 는 사람 결정 대기 — 에이전트가 닫지 않는다.
- **2026-09-18 16:15 증분 1(검수 표면) main 머지 — PR #41 `e80fbc1`(사용자가 직접 머지).** 브랜치 dispatch 3회 끝에 성공(run 35322958793): 판정 14건 → 403 출처 2건 제외 → 채점 2회 탈락 → **탈락 원고 PR #43** 이 후보 5·라벨과 함께 열림(설계대로 exit 0). 실전: #42 에 `/주제 3` → review.yml 이 재생성 통과·브랜치 갱신·탈락 라벨 제거(run 35323455831) · Vercel 봇 댓글은 OWNER 가드로 skip · hold-timer no-op. 실측으로 잡은 구멍 3개: 한도 오진(`c0d565d`) · 못 읽는 출처가 후보(`66acfa1`·`fe147ed` 문턱 1,500자) · 같은 다이제스트 재실행이 같은 출처를 또 고름(#42·#43 중복, 후속 PR `queued_urls`). **주의: 같은 작업 트리에서 세션 두 개가 병렬로 커밋·머지했다**(`34ded74` 는 다른 세션). 다음부터 병렬 작업은 worktree 로.
- **2026-09-18 18:40 증분 2 머지(#60 `ed59944`) + 채널 연결(#61 `7f59d3a`) + 1차 구현 묶음.** 브랜치 dispatch 두 번: 35327557882 ❌ 원고를 thread 브랜치에 커밋하고 돌아오면 파일이 작업 트리에서 사라져 본문 `cat` 이 죽음 → 본문을 먼저 만든다. 35328778638 ✅ 논평 PR #59(탈락, 9/21 월 발행 예정). 그 결과로 증분 1 버그 발견 — `/주제` 뒤 `← 이번 원고` 가 안 옮겨져 #59 가 #42 와 같은 출처 → `review_cmd.remark()`. 머지 후 publish-queue(dry)·slot-notify dispatch ✅(슬롯 메시지 원고 3건). 워크플로(서브에이전트 8개: 구현 4 + 적대 리뷰 4) → #56 자리표시 제목 · #57 뉴스레터 발송기 · #58 인스타 게시기(리뷰 차단 결함: 응답 단계 예외가 장부를 건너뜀 → 수정, 뮤턴트로 확인) · card-news#1 스모크 워크플로. 장부 `git add a b c` 는 하나만 없어도 전부 빠진다 — 파일마다. 다른 세션이 같은 레포에서 동시 작업 중이었다(#43·#44).
- **2026-09-19 증분 3 머지(#65 계기판 · #64 댓글 보조 · #66 슬롯 연결 `d1fc550`).** 결정 #53(수시 = 슬롯 일 20~21시 밖) · #54(a) 내 글의 미답 답글. 리뷰 major: 공개 레포 이슈에 남의 아이디·답글을 싣고 @멘션 → 전달을 Telegram 개인 채팅으로(Actions 로그도 공개라 스크립트가 직접 보내고 로그엔 건수만). 슬롯 jsonl 커밋은 최신 main 위에 파일만 얹는다(HEAD 를 밀면 브랜치 커밋이 main 에 샌다). **토 06:00 첫 cron(run 35403957903, 실제 08:00 시작 — GitHub 지연 2h)이 새 코드로 돌아 #62 블로그(시도 2 PASS)·#63 논평(9/21 월) 을 열었다.** 현 THREADS_TOKEN 은 `API access blocked`(OAuth 200) — 9/21 재발급.
- **2026-09-21 증분 5 헤드리스 스모크 통과 (card-news run 35553512316, 커밋 `1ddb994`)** — 미검증 ①이 풀렸다. ubuntu 러너에서 ① 렌더러가 픽스처 덱을 렌더(Playwright headless shell + 번들 Pretendard) ② `claude -p --dangerously-skip-permissions` 가 구독 토큰으로 Bash·Read·Write 를 쓰며 스스로 종료 ③ 런타임 심링크 `.claude/skills/card-news → skill/` 로 스킬 로더가 card-news 를 발견. 아직 안 본 것: ④ 주제 한 줄 → 덱 한 벌(리서치·Pexels·재시도 루프 포함, `full=true`). 같은 날 9/21 손작업 완료 확인 — seungbin-dev Secret 8개·card-news 3개, slot-notify dispatch 로 Telegram HTTP 200·Threads 인사이트 읽힘·구독자 7명.
